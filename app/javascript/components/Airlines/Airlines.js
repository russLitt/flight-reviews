import React, { useState, useEffect } from 'react'

const Airlines = () => {
  const [airlines, setAirlines] = useState([])

  useEffect(()=> {
      //get all airlines from api
      //update airlines in our state
  })
    return(
    <div>this is the Airlines#index view</div>
    )
}

export default Airlines